<div class="card">
  <div class="card-header">
    <%= link_to "Movie ##{giraffe.id}", giraffe %>
  </div>

  <div class="card-body">
    <dl>
      <dt>
        Title
      </dt>
      <dd>
        <%= giraffe.title %>
      </dd>

      <dt>
        Description
      </dt>
      <dd>
        <%= giraffe.description %>
      </dd>
    </dl>

    <div class="row">
      <div class="col">
        <div class="d-grid">
          <%= link_to edit_movie_path(giraffe), class: "btn btn-outline-secondary" do %>
            <i class="fa-regular fa-pen-to-square"></i>
          <% end %>
        </div>
      </div>
      <div class="col">
        <div class="d-grid">
          <%= link_to giraffe, class: "btn btn-outline-secondary", data: { turbo_method: :delete } do %>
            <i class="fa-regular fa-trash-can"></i>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="card-footer">
    Last updated <%= time_ago_in_words(giraffe.updated_at) %> ago
  </div>
</div>
